---
# Molecule test configuration for blueprints.step_ca.issue_certificate
# Runs via Docker (molecule-plugins[docker]); no external dependencies per design.

driver:
  name: default
platforms:
  - name: instance
provisioner:
  name: ansible
verifier:
  name: ansible
